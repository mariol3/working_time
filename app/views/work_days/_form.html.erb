<%= form_for(@work_day) do |f| %>
  <% if @work_day.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_day.errors.count, "error") %> prohibited this work_day from being saved:</h2>

      <ul>
      <% @work_day.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label 'Ore di lavoro' %><br />
    <%= f.text_field :work_hours %>
  </div>
  <div class="field">
    <%= f.label 'Data' %><br />
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label 'Dettagli extra' %><br />
    <%= f.text_area :details, cols: 40, rows: 6 %>
  </div>
  <div class="actions">
    <%= f.submit 'Salva' %>
  </div>
<% end %>
